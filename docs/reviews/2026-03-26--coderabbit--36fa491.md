# CodeRabbit Review — `36fa491`

- date: 2026-03-26
- repo: `betr-room-control-v4`
- base: `HEAD~1`
- verdict: `CLEAN`

## Scope Reviewed
- Preserve configured Discovery Server visibility in the wizard even when listener status is missing.
- Map configured-server/no-listener validation to listener startup trouble instead of `no_discovery_configured`.
- Restart the managed `BETRCoreAgent` when the operator uses `Apply + Restart` or `Start Over` restart.

## Final Rerun Result
- `coderabbit review --prompt-only -t uncommitted`
- Result: `Review completed: No findings`

## Manual Engineering Review
- Verdict: `PASS`
- This change matches the live operator complaint:
  - the wizard no longer throws away the configured Discovery Server in validation copy
  - the runtime summary keeps the configured server visible while discovery is degraded
  - `Apply + Restart` now prepares a real helper restart instead of only relaunching the shell

## Verification
- `BETR_CORE_DIR=/Users/joshperlman/Developer/betr/worktrees/betr-core-v3--phase2-media-proof swift test`
  - passed in `betr-room-control-v4`
- `BETR_CORE_DIR=/Users/joshperlman/Developer/betr/worktrees/betr-core-v3--phase2-media-proof ./scripts/build-app.sh --release-style`
  - passed in `betr-room-control-v4`
- `./scripts/validate-packaged-agent.sh --configuration release --expected-mode embeddedSMAppService`
  - passed in `betr-room-control-v4`
