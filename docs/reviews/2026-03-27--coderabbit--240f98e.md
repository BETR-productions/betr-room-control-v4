# CodeRabbit Review — `240f98e`

- Date: 2026-03-27
- Repo: `betr-room-control-v4`
- Base: `03afdaa`
- Verdict: `ESCALATED`

## Scope Reviewed
- UX-4 left-rail parity updates for:
  - restoring the V3 stacked left-rail structure with divider-separated sections
  - restoring operator-facing `RUN OF SHOW` copy and tighter card hierarchy
  - restoring the V3-style `CLIP PLAYER` panel structure without changing Clip Player producer behavior
  - restoring the V3-style `TIMER` panel grouping without changing Timer producer behavior

## CodeRabbit Findings
- Attempted local review command:
  - `REPO_DIR=/Users/joshperlman/Developer/betr/worktrees/betr-room-control-v4--ndi-discovery-debug BETR_ROOT=/Users/joshperlman/Developer/betr /Users/joshperlman/Developer/betr/scripts/coderabbit-local-review-common.sh --type committed --base-commit 03afdaa`
- Result:
  - CodeRabbit CLI reached `Reviewing` but never returned findings.
  - The local CLI log captured a repeat service/auth failure before completion:
    - `getSelfHostedPAT() called for SaaS auth, use getValidAccessToken() instead`
  - No usable findings were produced for this delta.

## Manual Engineering Review
- Verdict: `PASS`
- Release rationale:
  - the shipped left rail had drifted from the preserved V3 operator contract even though the underlying Clip Player and Timer behavior was already present
  - the current shell minimum width was not the main defect; the flattened V4 panel composition was causing the visible crowding and wrapped controls
  - this patch restores the V3 panel rhythm and copy while leaving Clip Player routing, Timer routing, discovery, and output ownership on the current V4 path
  - the change is intentionally isolated to the shell and status docs, which lowers release risk for the active NDI proof work

## Verification
- `BETR_CORE_DIR=/Users/joshperlman/Developer/betr/worktrees/betr-core-v3--phase2-media-proof swift test`
