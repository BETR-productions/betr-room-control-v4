# CodeRabbit Review — `424549b`

- Date: 2026-03-31
- Repo: `betr-room-control-v4`
- Base: `83b9628`
- Verdict: `ESCALATED`

## Scope Reviewed
- House-timed Room Control app changes for:
  - operator-facing house output format presets
  - workspace/client propagation of the configured output format
  - `PVW` driven from queue-backed pending readiness instead of the older flapping warm heuristic
  - live-program thumbnail alignment to the authoritative output surface owned by core

## CodeRabbit Result
1. Local CodeRabbit was available and authenticated for this release pass.
2. During the paired core review, the CLI surfaced several concrete findings that were fixed, then repeatedly stalled after `Reviewing` on reruns.
3. Because of that instability, no trustworthy separate automated verdict was captured for this merged app head.
4. This review is escalated for tool reliability reasons, not because an app-side blocking defect is currently known.

## Manual Engineering Review
- Verdict: `PASS WITH NOTES`
- What now matches the intended operator/runtime contract:
  - the app exposes the configured house format instead of treating the output as fixed `1080p29.97`
  - output `PVW` now reflects real take-readiness coming from the helper/runtime path, not a UI-owned timer or synthetic health rule
  - the app still does not own media cadence, transport timing, or extra polling; it consumes helper-owned output truth
  - the release path remains aligned with the standard NDI SDK model: one live program surface, one sender path, no loopback preview invention
- Remaining note:
  - live room proof is still the important post-build gate: confirm smooth motion, correct meters, and audible solo/program behavior against a real source/output chain on the published build

## Verification
- `BETR_CORE_DIR=/Users/joshperlman/Developer/betr/worktrees/betr-core-v3--land-house-timed-release swift test --package-path /Users/joshperlman/Developer/betr/worktrees/betr-room-control-v4--land-house-timed-release`
- `git -C /Users/joshperlman/Developer/betr/worktrees/betr-room-control-v4--land-house-timed-release diff --check`
